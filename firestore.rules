rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny ALL client-side access.
    // All reads/writes are performed server-side via the Admin SDK,
    // which bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
